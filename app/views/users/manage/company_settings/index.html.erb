
<main class="column">
    <div class="level">
        <div class="level-left">
        <div class="level-item">
            <div class="title">Company Information</div>
        </div>
        </div>
        <div class="level-right">
        <div class="level-item">
            <button type="button" class="button is-small">
            March 8, 2017
            </button>
        </div>
        </div>
    </div>

     <% if current_user.company_id.present? %>
     <%= form_for(@company, url: users_manage_company_setting_path(@company.id)) do |f|%>
        <%= render '/users/manage/company_settings/shared/form', f: f%>
    <% end %>
    
    <% else %>
   <%= form_for(@company, url: users_manage_company_settings_path , method: "post") do |f|%>
        <%= render '/users/manage/company_settings/shared/form', f: f%>
    <% end %>
    <% end %>

    
</main>
<script>
    <%= render '/users/manage/company_settings/shared/form.js'%>
</script>
