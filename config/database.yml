# PostgreSQL. Versions 9.3 and up are supported.
#
# Install the pg driver:
#   gem install pg
# On macOS with Homebrew:
#   gem install pg -- --with-pg-config=/usr/local/bin/pg_config
# On macOS with MacPorts:
#   gem install pg -- --with-pg-config=/opt/local/lib/postgresql84/bin/pg_config
# On Windows:
#   gem install pg
#       Choose the win32 build.
#       Install PostgreSQL and put its /bin directory on your path.
#
# Configure Using Gemfile
# gem 'pg'
#
default: &default
  adapter: postgresql
  encoding: unicode
  # For details on connection pooling, see Rails configuration guide
  # https://guides.rubyonrails.org/configuring.html#database-pooling
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  username: nbstnrvxrunsak
  password: 80219b1f608529a714c451ab9161457b1ebb0b80736b7376916aca56ed63ca87
  host: ec2-107-20-185-16.compute-1.amazonaws.com
  port: 5432
  database: d5c3b86qdgr7im

test:
  <<: *default
  database: surveyapp_test
 
production:
  <<: *default
  database: surveyapp_production
  username: surveyapp
  password: <%= ENV['SURVEYAPP_DATABASE_PASSWORD'] %>
